/* eslint-disable */
import { Text } from 'react-native';
import { withThemes } from '@southem/theme';

export const Message = withThemes('Message')(Text);
export const Label = withThemes('Label')(Text);

/* eslint-disable */
import { Animated } from 'react-native';
import { withThemes } from '@southem/theme';

const InputContainer = withThemes('InputContainer')(Animated.View);

export default InputContainer;

/* eslint-disable */
import { Text as RNText } from 'react-native';
import { withThemes } from '@southem/theme';

const DividerLabel = withThemes('DividerLabel')(RNText);

export default DividerLabel;

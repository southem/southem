import { transparent } from '../../colors';
import { fontSizeSubheading } from '../../spacing';
import palette from '../_palette';

export default {
  Subtitle: {
    color: palette.secondary,
    backgroundColor: transparent,
    fontSize: fontSizeSubheading,
    marginBottom: 2,
  },
};

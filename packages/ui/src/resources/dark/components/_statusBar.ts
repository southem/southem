import { getStatusBarHeight } from '../../../tools';

export default {
  StatusBar: {
    height: getStatusBarHeight(true),

    // Props
    // backgroundColor: 'red',
    barStyle: 'dark-content',
  },
};

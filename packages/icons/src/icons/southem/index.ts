import { IconPack } from '../../service';
import { IconProps } from '../../type';
import { createIconsMap } from './createIconsMap';

export const SouthemPack: IconPack<IconProps> = {
  name: 'southem',
  icons: createIconsMap(),
};

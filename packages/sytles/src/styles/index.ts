import {
  background,
  darkBackground,
  lightBackground,
} from './background';
import {
  border,
  darkBorder,
  lightBorder,
} from './border';
import {
  button,
  darkButton,
  lightButton,
} from './button';
import {
  text,
  darkText,
  lightText,
} from './text';

export default {
  // background
  background,
  darkBackground,
  lightBackground,

  // border
  border,
  darkBorder,
  lightBorder,

  // button
  button,
  darkButton,
  lightButton,

  // text
  text,
  darkText,
  lightText,
};

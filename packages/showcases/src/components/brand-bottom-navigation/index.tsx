import React from 'react';
import { BottomNavigation, BottomNavigationProps } from '@southem/ui';

export const BrandBottomNavigation = (props: BottomNavigationProps): React.ReactElement => {
  return (
    <BottomNavigation {...props}/>
  );
};

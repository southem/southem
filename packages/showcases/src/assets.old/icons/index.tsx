import React from 'react';
import {
  ImageProps,
  ImageStyle,
  StyleProp,
} from 'react-native';
import {
  AssetIcon,
  AssetIconElement,
  IconSource,
  // @ts-ignore
  RemoteIcon,
} from './icon.component';
import {
  Icon,
  IconElement,
} from '@southem/ui';

export const AssetAutocompleteIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-autocomplete.png'),
  };

  return AssetIcon(source, style);
};

export const AssetAutocompleteDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-autocomplete-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetAvatarIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-avatar.png'),
  };

  return AssetIcon(source, style);
};

export const AssetAvatarDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-avatar-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetButtonIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-button.png'),
  };

  return AssetIcon(source, style);
};

export const AssetButtonDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-button-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetButtonGroupIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-button-group.png'),
  };

  return AssetIcon(source, style);
};

export const AssetButtonGroupDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-button-group-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetBottomNavigationIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-bottom-navigation.png'),
  };

  return AssetIcon(source, style);
};

export const AssetBottomNavigationDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-bottom-navigation-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetCalendarIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-calendar.png'),
  };

  return AssetIcon(source, style);
};

export const AssetCalendarDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-calendar-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetCardIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-card.png'),
  };

  return AssetIcon(source, style);
};

export const AssetCardDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-card-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetCheckBoxIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-checkbox.png'),
  };

  return AssetIcon(source, style);
};

export const AssetCheckBoxDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-checkbox-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetDatepickerIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-datepicker.png'),
  };

  return AssetIcon(source, style);
};

export const AssetDatepickerDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-datepicker-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetDrawerIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-drawer.png'),
  };

  return AssetIcon(source, style);
};

export const AssetDrawerDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-drawer-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetIconIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-icon.png'),
  };

  return AssetIcon(source, style);
};

export const AssetIconDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-icon-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetInputIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-input.png'),
  };

  return AssetIcon(source, style);
};

export const AssetInputDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-input-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetListIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-list.png'),
  };

  return AssetIcon(source, style);
};

export const AssetListDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-list-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetOverflowMenuIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-overflow-menu.png'),
  };

  return AssetIcon(source, style);
};

export const AssetOverflowMenuDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-overflow-menu-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetPopoverIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-popover.png'),
  };

  return AssetIcon(source, style);
};

export const AssetPopoverDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-popover-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetRadioIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-radio.png'),
  };

  return AssetIcon(source, style);
};

export const AssetRadioDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-radio-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetTabViewIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-tab-view.png'),
  };

  return AssetIcon(source, style);
};

export const AssetTabViewDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-tab-view-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetTextIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-text.png'),
  };

  return AssetIcon(source, style);
};

export const AssetTextDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-text-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetToggleIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-toggle.png'),
  };

  return AssetIcon(source, style);
};

export const AssetToggleDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-toggle-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetTooltipIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-tooltip.png'),
  };

  return AssetIcon(source, style);
};

export const AssetTooltipDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-tooltip-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetTopNavigationIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-top-navigation.png'),
  };

  return AssetIcon(source, style);
};

export const AssetTopNavigationDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-top-navigation-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetMenuIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-menu.png'),
  };

  return AssetIcon(source, style);
};

export const AssetMenuDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-menu-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetModalIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-modal.png'),
  };

  return AssetIcon(source, style);
};

export const AssetModalDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-modal-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetSelectIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-select.png'),
  };

  return AssetIcon(source, style);
};

export const AssetSelectDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-select-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetSpinnerIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-spinner.png'),
  };

  return AssetIcon(source, style);
};

export const AssetSpinnerDarkIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-spinner-dark.png'),
  };

  return AssetIcon(source, style);
};

export const AssetAuthIcon = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-auth.png'),
  };

  return AssetIcon(source, style);
};

export const AssetAuthIconDark = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-auth-dark.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconSocial = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-social.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconSocialDark = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-social-dark.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconArticles = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-articles.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconArticlesDark = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-articles-dark.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconMessaging = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-messaging.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconMessagingDark = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-messaging-dark.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconDashboards = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-dashboards.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconDashboardsDark = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-dashboards-dark.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconEcommerce = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-ecommerce.png'),
  };

  return AssetIcon(source, style);
};

export const MenuIconEcommerceDark = (style: StyleProp<ImageStyle>): AssetIconElement => {
  const source: IconSource = {
    imageSource: require('./icon-ecommerce-dark.png'),
  };

  return AssetIcon(source, style);
};

export const ArrowHeadDownIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} type='material-community' name='arrowhead-down'/>
  );
};

export const ArrowHeadUpIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='arrowhead-up'/>
  );
};

export const ArrowIosBackFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='arrow-back'/>
  );
};

export const BulbIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='bulb'/>
  );
};

export const CameraIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='camera'/>
  );
};

export const CartIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='shopping-cart-outline'/>
  );
};

export const CartIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='shopping-cart'/>
  );
};

export const ClockIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='clock-outline'/>
  );
};

export const EmailIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='email'/>
  );
};

export const EyeOffIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='eye-off'/>
  );
};

export const ColorPaletteIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='color-lens'/>
  );
};

export const ArrowForwardIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='arrow-forward-outline'/>
  );
};

export const GridIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='grid-outline'/>
  );
};

export const HeartIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='heart'/>
  );
};

export const LayoutIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='view-compact'/>
  );
};

export const LockIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='lock'/>
  );
};

export const MessageCircleIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='message-circle'/>
  );
};

export const MessageCircleIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='message-circle-outline'/>
  );
};

export const MoreHorizontalIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='more-horiz'/>
  );
};

export const PersonIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='person'/>
  );
};

export const PhoneIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='phone'/>
  );
};

export const PersonAddIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='person-add'/>
  );
};

export const PinIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='pin'/>
  );
};

export const PinIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='pin-outline'/>
  );
};

export const PlusIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='plus'/>
  );
};

export const MinusIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='minus'/>
  );
};

export const SearchIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='search-outline'/>
  );
};

export const StarIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='star'/>
  );
};

export const StarIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='star-border'/>
  );
};

export const FlashIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} type='material-community' name='flash-outline'/>
  );
};

export const DoneAllIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='done-all'/>
  );
};

export const MoreVerticalIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='more-vert'/>
  );
};

export const CreditCardIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='payment'/>
  );
};

export const CloseIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='close-outline'/>
  );
};

export const MicIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='mic'/>
  );
};

export const PaperPlaneIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='paper-plane'/>
  );
};

export const ImageIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='image'/>
  );
};

export const FileTextIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='file-text'/>
  );
};

export const MapIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='map'/>
  );
};

export const PeopleIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='people'/>
  );
};

export const PlayCircleIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='play-circle'/>
  );
};

export const ShareIconOutline = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='share-outline'/>
  );
};

export const ListIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='list'/>
  );
};

export const GoogleIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='google'/>
  );
};

export const FacebookIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='facebook'/>
  );
};

export const TwitterIconFill = (style: ImageStyle): IconElement<ImageProps> => {
  return (
    <Icon style={style} name='twitter'/>
  );
};

export {
  AssetIcon,
  IconSource,
  RemoteIcon,
} from './icon.component';
